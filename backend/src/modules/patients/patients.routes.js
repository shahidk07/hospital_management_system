import express from 'express';
const router = express.Router();

// Stub for patient management routes (owned by Sanskar Agrawal)
router.get('/', (req, res) => res.json({ message: 'Patient module stub' }));

export default router;
