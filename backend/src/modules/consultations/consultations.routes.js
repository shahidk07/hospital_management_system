import express from 'express';
const router = express.Router();

// Stub for consultation routes (owned by Dakshesh Jain)
router.get('/', (req, res) => res.json({ message: 'Consultation module stub' }));

export default router;
