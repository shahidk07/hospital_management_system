import express from 'express';
const router = express.Router();

// Stub for notifications routes (owned by Sanskar Agrawal)
router.get('/', (req, res) => res.json({ message: 'Notifications module stub' }));

export default router;
