import express from 'express';
const router = express.Router();

// Stub for appointment management routes (owned by Sanskar Agrawal)
router.get('/', (req, res) => res.json({ message: 'Appointment module stub' }));

export default router;
